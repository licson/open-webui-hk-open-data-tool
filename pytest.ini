[pytest]
testpaths = tests
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
addopts = -m "not live" --strict-markers -q
markers =
    live: real-network smoke tests (deselected by default; run with -m live)
